export enum TransactionStatus {
  CREATED = "CREATED",
  PENDING_3DS = "PENDING_3DS",
  SUCCESS = "SUCCESS",
  FAILED = "FAILED",
}

export enum Currency {
  USD = "USD",
  EUR = "EUR",
  GBP = "GBP",
}
